const initialState = {
  name: "Sai Kishan",
  email: "sonusaikishan@gmail.com"
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
